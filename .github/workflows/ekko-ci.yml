name: Ekko CI

on:
  push:
    branches: ["main"]
    paths:
      - "services/ekko/*"
  pull_request:
    branches: ["*"]
    paths:
      - "services/ekko/*"

jobs:
  test:
    uses: johnvicke/lol-data-tracker/.github/workflows/hello-world.yml
    with:
      foo: "lol"
